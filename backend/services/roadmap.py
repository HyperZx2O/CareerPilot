def generate_roadmap(goal: str, cv_chunks) -> List[str]:
    # Placeholder: return static steps
    return [
        f"Assess current skills for {goal}",
        "Identify gaps",
        "Select learning resources",
        "Create weekly schedule",
        "Track progress"
    ]
